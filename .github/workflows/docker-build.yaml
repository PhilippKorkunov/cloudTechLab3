name: Docker Build

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v2

      - name: m Start
        run: minikube start

       - name: m Start2
        run: |
        kubbectl applay -f deployment.yaml deployment.aps/examle-app-created